  <p id="notice"><%= notice %></p>

  <h2> Olá, <%= current_paciente.nome_completo %></h2>

  <%= link_to 'Visualizar', current_paciente %> |
  <%= link_to 'Editar Dados', edit_paciente_path(current_paciente)%>
  <hr>

  <h1>Agendamento de Consultas</h1>

  <table>
    <thead>
      <tr>
        <th>Nome do Médico</th>
        <th>Data</th>
        <th>Horario</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @consultas.each do |consulta| %>
        <tr>
          <td><%= consulta.nome_medico %></td>
          <td><%= consulta.data %></td>
          <td><%= consulta.horario %></td>
          <td><%= link_to 'Show', consulta %></td>
          <td><%= link_to 'Edit', edit_consulta_path(consulta), id:"e-#{consulta.nome_medico}" %></td>
          <td><%= link_to 'Destroy', consulta, method: :delete, id:"d-#{consulta.nome_medico}" %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
  <%= link_to 'Nova Consulta', new_consulta_path %><br><br><br>
  <hr><hr>
  <%= link_to "Sair", sair_path, method: :delete %>